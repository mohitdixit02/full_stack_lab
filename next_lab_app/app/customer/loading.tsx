export default function Loading() {
  // Or a custom loading skeleton component
  return (
    <div>
        <h2>Loading... (Layout.tsx)</h2>
        <p>
            This loading component will be displayed while the data is being fetched from the API route. <br/>
            It is shown for all pages under the /customer route when they are in loading state. <br/>
        </p>
    </div>
  )
}