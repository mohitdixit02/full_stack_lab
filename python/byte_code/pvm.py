def get_pvm():
    return "pvm"

# byte code file - pvm.cpython-313.pyc
# <file>.<python implementation>-<python version>.pyc

# Byte code is generated for imports as they have higher chance of being reused, but not for the main code,
# as it is run only once per execution.
